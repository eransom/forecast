{
  "name": "forecast",
  "version": "1.0.0",
  "description": "project that displays weather",
  "main": "index.js",
  "scripts": {
    "test": "mocha test/*",
    "bundle": "browserify main.js -o bundle.js",
    "watch": "watchify main.js -o bundle.js",
    "deploy": "gh-pages -d dist"
  },
  "author": "Eric Ransom",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "mocha": "^3.2.0",
    "watchify": "^3.7.0"
  },
  "dependencies": {
    "jquery": "^3.1.1"
  }
}
